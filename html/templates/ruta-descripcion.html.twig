<div class="tab-pane fade show active" id="list-home" role="tabpanel" aria-labelledby="list-home-list">

    <div class="form-row">
        <div class="form-group col">
            <label for="inputState">Pais</label>
            <select id="inputState" class="form-control">
                {% for x in paises.paises %}
                    <option> {{x.nombre}}</option>
                {% endfor %}
            </select>
        </div>
        <div class="form-group col">
            <label for="inputState">Ciudad</label>
            <select id="inputState" class="form-control">
                {% for x in paises.paises %}
                    {% for x in x.ciudades %}
                        <option> {{x.nombre_ciudad}}</option>
                    {% endfor %}
                {% endfor %}
            </select>
        </div>
    </div>

    <div class="form-row">
        <div class="form-group col">
            <label for="inputState">Hotel</label>
            <select id="inputState" class="form-control">
                <option selected>Choose...</option>
                <option>...</option>
            </select>
        </div>
        <div class="form-group col">
            <label for="inputState">Transporte</label>
            <select id="inputState" class="form-control">
                <option selected>Choose...</option>
                <option>...</option>
            </select>
        </div>
    </div>

    <div class="form-row">
        <div class="form-group col">
            <label for="inputState">Paquete hotelero</label>
            <select id="inputState" class="form-control">
                {% for x in paises.paises %}
                    <option> {{x.nombre}}</option>
                {% endfor %}
            </select>
        </div>
        <div class="form-group col">
            <label for="inputState">Tipo de transporte</label>
            <select id="inputState" class="form-control">
                <option selected>Choose...</option>
                <option>...</option>
            </select>
        </div>
    </div>

    <div class="row">
        <div class="col">
            <h5 class="d-inline">Guías</h5>
            <a href="#" type="button d-inline" class="btn btn-primary btn-sm">Agregar Guia</a>
            <div class="list-group overflow-auto mt-3" id="list-tab" style="height: 240px" role="tablist">
                {% for x in y %}
                {% include 'ruta.html.twig' with {'nombre': x} %}
                {% endfor %}
            </div>
        </div>
        <div class="col">
            <h5 class="d-inline">Lugares</h5>
            <a href="#" type="button d-inline" class="btn btn-primary btn-sm">Agregar Lugar</a>
            <div class="list-group overflow-auto mt-3" id="list-tab" style="height: 240px" role="tablist">
                {% for x in y %}
                {% include 'ruta.html.twig' with {'nombre': x} %}
                {% endfor %}
            </div>
        </div>
    </div>
    
</div>